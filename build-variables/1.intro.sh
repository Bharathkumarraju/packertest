The build command takes an extra flags.
one of which is the var flag (or) variable flag.

The var flag allows for variables to passed in when running a build.

An example of variables would be an AWS key and secret, something you do not want to hard code into the template,but use it when building.

so we can input these secrets as variables when buulding a template.

so lets set the AWS access key variable into this template.


